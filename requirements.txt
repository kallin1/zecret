streamlit
pydeck  # 관제센터 대시보드의 계획 건물 위치 지도 렌더링용 — VWORLD_API_KEY 설정 시 VWorld 실제 건물 타일(TileLayer), 미설정 시 라벨 없는 dark_no_labels 스타일로 폴백
tenseal==0.3.16  # 군사시설 카테고리 실제 CKKS 연산 (src/he/encryption.py, scripts/generate_mock_ciphertexts.py, scripts/mock_authority_verify.py). CKKS 지원 + 이 환경(Python 3.13/Windows)에 prebuilt wheel 존재 (Pyfhel은 sdist만 있어 소스 빌드 필요해 보류). 버전 고정 이유: generate_mock_ciphertexts.py의 CKKS 파라미터(coeff_mod_bit_sizes/global_scale)는 이 버전 기준으로 노이즈를 실측 검증했음 — 버전이 바뀌면 직렬화/rescale 동작 차이로 복호화 결과가 달라질 수 있음
langgraph  # 컴플라이언스 판정 파이프라인 그래프 오케스트레이션 (src/graph)
langfuse   # 노드별 실행 span 트레이싱 — 민감 필드는 redact 래퍼(src/graph/tracing.py)로 마스킹 후 기록
requests  # llm_summarize_node/AI Agent 채팅의 LLM 호출 (src/graph/llm_client.py) — NCloud CLOVA Studio(HyperCLOVA X) REST API 호출용
python-dotenv
pytest  # 판정 로직 테스트 실행용
chromadb   # RAG 벡터DB — 고시 조문 청크 색인/조회 (src/rag), 판정에는 관여하지 않고 근거 인용 전용