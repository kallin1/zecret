# CLAUDE.md

> 프로젝트명: **ZeCret**

이 파일은 Claude Code가 이 저장소에서 작업할 때 반드시 참고해야 할 프로젝트 지침입니다.
전체 배경은 `docs/기획서_v2.docx`, 사용자 대상 설명은 `README.md`를 참고하세요.

## 프로젝트 한 줄 요약

프로젝트명: **ZeCret**. 군사시설 등 공개제한구역의 **높이(Z)값**을 CKKS 동형암호로 암호화한 상태로 유지한 채,
사용자가 입력한 신축 건물 정보와 비교하여 **일조권 침해·기준 초과 여부만 판정**해 반환하는
2주짜리 PoC 서비스. UI는 Streamlit, 배포는 AWS.

## 절대 원칙 (하드 제약 — 위반 금지)

이 프로젝트의 핵심 가치는 "높이 정보를 노출하지 않고도 판단 결과를 준다"는 것입니다.
아래 원칙은 성능/편의를 이유로 절대 우회하지 않습니다.

1. **원본 Z값(고도·높이)은 어떤 API 응답에도, 로그에도, 프론트엔드로도 평문 그대로 나가면 안 됩니다.**
   복호화는 서버 내부 판정 로직 안에서만 일어나고, 결과는 boolean/등급/토큰으로만 반환합니다.
2. **클라이언트는 원본 값도 암호문 자체도 받지 않습니다.** 받는 건 참조 토큰뿐입니다.
   토큰 형식: `HE:{datasetId}:{buildingIndex}` — 이 값으로 원본을 재구성할 수 없어야 합니다.
3. **X, Y(위치)는 평문으로 취급해도 됩니다.** 암호화 대상은 Z(높이·고도)뿐입니다. 이 구분을 헷갈리지 마세요.
4. **지도 시각화는 정밀 좌표/높이 대신 격자(grid) 단위 위험도만 표시**합니다. 개별 건물의 정확한 형태·높이를 그대로 그리지 않습니다.
5. AI Agent가 LLM 응답을 생성할 때, **실제 판정 함수가 반환한 값에 근거해서만** 자연어 요약을 만들어야 합니다.
   LLM이 판정 결과를 임의로 지어내는 것은 금지입니다 (function calling 결과를 그대로 요약하는 방식으로 구현).

체크포인트 4종(①암호화 저장 ②범위검색 ③시각화 대안 ④토큰 참조값)은 위 원칙을 코드로 구현한 것입니다.
새 기능을 추가할 때 이 체크포인트 중 하나라도 깨지지 않는지 항상 확인하세요.

## 기술 스택

| 영역 | 사용 기술 |
|---|---|
| 언어 | Python 3.10+ |
| UI | Streamlit |
| 동형암호 | CKKS 기반 라이브러리 (Pyfhel 우선 검토, 어려우면 OpenFHE) |
| 공간정보 | V-World 오픈API (건물/DEM/행정구역) |
| 기상/일조 | 기상청 오픈API |
| AI Agent | Claude API function calling (자연어 → 파라미터 추출 → 함수 호출 → 결과 요약) |
| RAG | 법령 문서(보안관리규정 등) 임베딩 기반 질의응답, 벡터 검색 |
| 배포 | 단일 Dockerfile로 패키징한 컨테이너를 AWS EC2 또는 ECS에 배포, 데이터/결과는 S3 |

## 폴더 구조 (권장)

```
.
├── CLAUDE.md
├── README.md
├── docs/
│   └── 기획서_v2.docx
├── app.py                     # Streamlit 진입점
├── data/
│   ├── restricted_facilities.csv   # 가상 공개제한시설 데이터 (X,Y,Z)
│   └── sample_dem/                 # V-World/기상청에서 받은 배경 데이터 캐시
├── src/
│   ├── he/
│   │   ├── encryption.py      # CKKS 암호화/복호화 (서버 내부 전용)
│   │   └── compare.py         # 암호문-평문 혼합 연산, 기준 초과 판정
│   ├── geo/
│   │   ├── range_search.py    # BBOX/반경 범위검색 (체크포인트②)
│   │   ├── sunlight.py        # 일조량·그림자 계산
│   │   └── grid_render.py     # 격자 기반 시각화, 서버 렌더링 (체크포인트③)
│   ├── tokens.py               # HE:datasetId:index 토큰 발급/검증 (체크포인트④)
│   ├── agent/
│   │   ├── tools.py            # AI Agent가 호출할 함수 정의
│   │   └── router.py           # 자연어 요청 → Agent 또는 RAG로 라우팅
│   └── rag/
│       ├── ingest.py           # 법령 문서 임베딩
│       └── qa.py               # 법령 질의응답
├── tests/
├── requirements.txt
└── Dockerfile                  # app.py + src/he,geo,agent,rag 전체를 단일 컨테이너로 패키징
```

배포는 도메인별로 컨테이너를 나누지 않고, 루트의 `Dockerfile` 하나로 `app.py`와 `src/` 전체
(HE/geo/agent/rag 모듈 포함)를 하나의 컨테이너 이미지로 패키징합니다. `src/he`, `src/geo`,
`src/agent`, `src/rag` 폴더 구조 자체는 그대로 유지하고, 통합은 컨테이너 레벨에서만 합니다.

아직 코드가 없다면 이 구조를 기준으로 새로 만들어도 됩니다. 다만 `he/` 모듈 밖으로
평문 Z값이나 복호화 로직이 새어나가지 않도록 주의하세요 (원칙 1, 2 위반 여지).

## 개발 우선순위 (2주 일정 기준)

기획서의 일정과 동일한 순서로 작업하세요. 뒷 단계 기능을 먼저 만들지 않습니다.

1. **Week 1 (1~2일차)**: V-World/기상청 API 연동, 가상 공개제한시설 데이터셋 생성, HE 라이브러리 셋업
2. **Week 1 (3~4일차)**: Z값 CKKS 암호화 저장 (체크포인트①), 사용자 입력 폼(위치·높이)
3. **Week 1 (5~7일차)**: 범위검색 + 혼합연산 판정 로직 (체크포인트②), 일조량 계산
4. **Week 2 (8~9일차)**: 격자 시각화·서버 렌더링 (체크포인트③), 토큰 참조 응답 (체크포인트④)
5. **Week 2 (10~11일차)**: AI Agent, RAG 연동
6. **Week 2 (12일차)**: AWS 배포, 성능 최적화
7. **Week 2 (13~14일차)**: 통합 테스트, 발표자료 준비

체크포인트 ①~④가 안정적으로 동작하기 전에는 AI Agent/RAG 작업에 시간을 쓰지 마세요.
핵심 암호화 파이프라인이 우선입니다.

## 성능 관련 지침

- 동형암호 파라미터는 **처음부터 크게 잡지 말고 작게 시작**하고, 정밀도가 부족할 때만
  부트스트래핑을 수행하세요. 파라미터 크기를 무작정 키우면 암호화 용량이 기하급수적으로 늘어납니다.
- 바이너리 단위 연산보다 **행렬 연산으로 벡터화**해서 처리하는 방향을 우선 검토하세요.
- 안심구역처럼 암호화되지 않은 데이터도 연산이 오래 걸릴 수 있는데, 이번 프로젝트에서
  처리 속도 자체는 핵심 평가 기준이 아니므로 정확성·구조를 먼저 완성하고 최적화는 나중에 하세요.

## 코딩 컨벤션

- 함수/변수명은 영어, 주석과 커밋 메시지는 한국어/영어 무관 (팀 컨벤션 따라 통일)
- 암호화 관련 코드(`src/he/`)에는 반드시 어떤 값이 평문이고 어떤 값이 암호문인지
  타입 힌트나 변수명(`_enc`, `_plain` 접미사 등)으로 명시
- Streamlit 화면 코드와 HE/판정 로직은 분리 (UI 코드에 암호화 로직을 직접 넣지 않기)
- 새 함수를 AI Agent의 tool로 노출할 때는 반드시 `src/agent/tools.py`에 등록하고,
  반환값은 판정 결과(요약 가능한 구조화 데이터)만 포함 — 원본 Z값 필드 포함 금지

## 참고 문서

- 전체 기획 배경: `docs/기획서_v2.docx` (체크포인트 대응 전략은 4장 참고)
- 사용자 대상 설명: `README.md`
- 국가공간정보 보안관리규정 등 법령 원문은 RAG 인제스트 시 `src/rag/ingest.py`에서 관리
