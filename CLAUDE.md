# CLAUDE.md
이 파일은 Claude Code가 이 저장소에서 작업할 때 반드시 참고해야 할 프로젝트 지침입니다.
사용자 대상 설명(README.md)이 바뀔 만한 수정을 할 경우, README.md도 함께 갱신할 것

## 프로젝트 한 줄 요약

프로젝트명: **ZeCret**. 민간 사업자의 신축 계획이 인접 공개제한구역(군사시설 등)의 암호화된 높이(Z)값 기준을 위반하는지만 판정해 반환하는 프로젝트.

- Z값은 CKKS 동형암호로 암호화된 상태를 유지하며, 서비스는 판정 결과(위반 여부)만 반환한다.
- 문화재 경관보호·일조권 사선제한은 기준치가 원래 공개된 수치이므로 동일 로직을 평문으로 재사용한다.
- 민간 대 민간 조망권은 법정 수치 기준이 없어 판정 대상에서 제외한다.

## 절대 원칙 (하드 제약 — 위반 금지)

이 프로젝트의 핵심 가치는 "높이 정보 비노출"이다. 아래 원칙은 성능·편의를 이유로 절대 우회하지 않는다.

> 체크포인트는 기획 단계에서 정한 4가지 평가 기준이다 —
> ①암호화 저장 ②범위검색 ③시각화 대안 ④토큰 참조값.
> 아래 원칙은 이 4가지를 구현 규칙으로 풀어낸 것이며, 원칙 5(LLM 임의 응답 금지)는
> 체크포인트와 별개로 AI Agent 구현 시 추가로 지켜야 하는 규칙이다.

1. **원본 Z값(고도·높이)은 어떤 API 응답에도, 로그에도, 프론트엔드로도 평문 그대로 나가서는 안된다.** (체크포인트 ①)
   본 서비스(app.py, src/ 전체)는 비밀키를 보유하지 않는다. 서비스가 수행 가능한 연산은 암호문(Z값) - 평문(계획높이)의 동형 뺄셈까지이며, 그 결과(diff 암호문)는 절대 서버 내부에서도 복호화하지 않는다. 복호화(부호 비트만)는 비밀키를 보유한 관리기관 측 HSM 검증 API에서 수행하고, 서비스는 그 이진 응답만 전달받는다. 판정 결과 반환 스키마는 {"exceeds_limit": bool, "margin": float | None}로 통일한다. 공개제한(HE) 대상은 margin이 항상 None이며, 문화재/일조권(평문) 대상만 실제 margin 값을 채운다.
2. **클라이언트는 원본 값도 암호문 자체도 받지 않는다.** (체크포인트 ④)
   받는 건 참조 토큰뿐이다. 토큰 형식: `HE:{datasetId}:{buildingIndex}` — 이 값으로 원본을 재구성할 수 없어야 한다.
3. **X, Y(위치)는 평문으로 취급해도 된다.** 암호화 대상은 Z(높이·고도)뿐이다. 이 구분을 헷갈려서는 안 된다.
4. **지도 시각화는 정밀 좌표/높이 대신 격자(grid) 단위 위험도만 표시** (체크포인트 ③)
   개별 건물의 정확한 형태·높이를 그대로 그리지 않는다.
5. **AI Agent가 LLM 응답을 생성할 때, 실제 판정 함수가 반환한 값에 근거해서만 자연어 요약을 만들어야 한다.**
   LLM이 판정 결과를 임의로 지어내는 것은 금지 (function calling 결과를 그대로 요약하는 방식으로 구현).
   — *체크포인트와 무관하게, AI Agent 구현 시 지켜야 할 별도 원칙이다.*

새 기능을 추가할 때는 위 5개 원칙 중 하나라도 깨지지 않는지 항상 확인한다.

## 기술 스택 관련 참고

- 사용 라이브러리·버전은 `requirements.txt` 참고
- 동형암호 라이브러리는 현재 **TenSEAL**(CKKS)을 잠정 채택 — 대상 환경(Python 3.13/Windows)에 prebuilt wheel이
  있어 빠른 구현이 가능하기 때문. Pyfhel/OpenFHE는 소스 빌드가 필요해 보류 중이며 교체 가능성이 있으므로,
  최종 확정 여부는 `requirements.txt`가 기준
- 배포는 도메인별 컨테이너 분리 없이 `Dockerfile` 하나로 app.py + src/ 전체(he/geo/agent/rag)를 패키징한다.
  통합은 컨테이너 레벨에서만 하고, `src/` 하위 모듈 구조 자체는 그대로 유지한다.
- 데이터 저장소는 역할별로 4개로 분리한다: 구조화 기준값 DB(src/db — 일조권/문화재/군사시설 기준치, 정확값 대조용), RAG 벡터DB(src/rag — 고시 원문, 근거 인용 전용, 판정에는 관여하지 않음), 암호문 캐시(원본 Z값은 저장하지 않고 ciphertext_blob + he_context_version만 opaque하게 보관), Langfuse 트레이싱(민감 필드는 redact 후 메타데이터만 로깅).
- 그래프 오케스트레이션은 LangGraph로 구현한다. 노드 구성: search_zone_node → (조건부 분기) → he_compute_node → authority_verify_node / plain_compute_node → rag_check_node → llm_summarize_node. HE 경로만 authority_verify_node를 거친다.
  이 단일-진입 그래프(`src/graph/build.py`)는 계획 건물 1건당 시설 1건만 판정하는 구조라 `tests/test_graph_end_to_end.py`용 레퍼런스/테스트 경로로 유지한다.
  **실제 Streamlit 앱(`app.py`)이 쓰는 경로는 `src/graph/runner.py`의 `run_full_compliance_check()`이며, 이 함수는 search_zone_node를 거치지 않는다.**
  대신 일조권 사선제한은 항상 1건, 국가유산은 `ADJACENCY_RADIUS_M` 반경 안에 있는 시설마다, 군사시설은 시설 유형별 반경(`config.zone_radius_m()`, 군사기지법 제5조 지정범위 근거)에 걸리는 시설마다 각각 서브그래프(`_PLAIN_SUBGRAPH`/`_MILITARY_SUBGRAPH`)를 실행해 여러 건을 동시에 판정·반환한다 — 한 건물이 세 카테고리를 동시에 위반할 수 있어야 하기 때문이다. 새 카테고리를 추가하거나 판정 흐름을 바꿀 때는 `runner.py` 쪽을 기준으로 삼는다.
- **군사시설은 (facility_id, regulation_theme) 복합키로 규정 테마를 구분한다.** 같은 시설(서울공항)이라도 군사기지법 제9조(제한보호구역)와 제10조(비행안전구역)가 중첩 적용되어 서로 다른 비공개 Z값을 가지므로, `MilitaryZone.regulations`(리스트)에 테마별 `MilitaryRegulationTheme`(자체 암호문)를 두고 `he_compute_node`/`db.queries`/`rag.qa`가 전부 `regulation_theme`을 키로 조회한다. 새 규정 테마를 추가할 때는 `src/db/schema.py`(구조화 DB)·`src/db/ciphertext_cache.py`(암호문 캐시)·`scripts/generate_mock_ciphertexts.py`·`src/rag/ingest.py` 네 곳 모두 같은 (facility_id, regulation_theme) 키로 맞춰야 한다.
- **군사시설/국가유산은 성남시 실재 시설(서울공항·남한산성)에 좌표·법적 근거를 접지한다.** 다만 높이제한 수치(Z)는 안보상/개별고시 비공개라 여전히 임의값이다 — 실제로 확인된 것은 시설 정체성·위치·근거 조문(군사기지법 제9·10조)뿐이며, 조사 근거는 `src/compliance/config.py`/`src/rag/ingest.py` 주석 참고.
- 지도 시각화 보조 로직(`src/geo`)은 판정과 무관한 순수 배경/보조 레이어다: `geo/risk_grid.py`(원칙 4의 격자 위험도 계산), `geo/buildings.py`(VWorld 건물통합정보 WFS 2.5D 배경 — VWorld 3D Tiles 공개 API는 폐쇄되어 사용 불가하다는 사전 조사 결과에 따른 대안). 판정 결과에 관여하지 않으므로 이 모듈에 HE/판정 로직을 넣지 않는다.

## 성능 관련 지침
- 동형암호 파라미터는 처음부터 크게 잡지 말고 작게 시작한다. 파라미터 크기를 무작정 키우면
  암호화 용량이 기하급수적으로 늘어난다.
- **부트스트래핑은 이번 범위에 포함하지 않는다.** 정밀도 부족이 의심되는 경우에도 클로드가
  임의로 부트스트래핑을 구현하지 말고, 파라미터 조정으로 해결 가능한지 먼저 확인한 뒤 사람에게
  알린다. 부트스트래핑 도입은 별도 논의·확정 후에만 진행한다.

## 코딩 컨벤션

- 암호화 관련 코드(`src/he/`)에는 어떤 값이 평문이고 어떤 값이 암호문인지
  타입 힌트나 변수명(`_enc`, `_plain` 접미사 등)으로 반드시 명시한다 (원칙 1·2 관련)
- Streamlit 화면 코드와 HE/판정 로직은 분리한다 (UI 코드에 암호화 로직을 직접 넣지 않는다)
- 새 함수를 AI Agent의 tool로 노출할 때는 반드시 `src/agent/tools.py`에 등록하고,
  반환값은 판정 결과(요약 가능한 구조화 데이터)만 포함한다 — 원본 Z값 필드 포함 금지 (원칙 1·2 관련)
- 일조권 사선제한의 이격거리(정북방향 인접대지경계선까지 거리)는 현재 GIS 경계선 데이터 연동 전이므로 사용자 입력값으로 받는다. 자동 산출(지적경계 폴리곤 기반)은 이후 별도 과제.